/// Max width of display content for the largest screens.
const largeScreenDisplayWidth = 1200.0;

/// Minimum width before we change the global responsive display (Tablet, small latop screen).
const mediumScreenCapWidth = largeScreenDisplayWidth + 100.0;

/// Minimum width before we change the global responsive display (Smartphone).
const smallScreenCapWith = 800.0;
